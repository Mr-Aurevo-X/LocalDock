{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LocalDock desktop window with core IPC only.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
